{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "events",
  "description": "Allow the frontend to listen to core events",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:event:allow-listen"
  ]
}